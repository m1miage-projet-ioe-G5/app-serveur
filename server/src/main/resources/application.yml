server:
  port: 8081
  servlet:
    context-path: /api

spring:
  application:
    name: projet-ioe

  datasource:
    url: jdbc:postgresql://dpg-d08h3pqli9vc739qjmtg-a:5432/moovly_db?sslmode=require
    username: moovly_db_user
    password: yszVEVrHB8xZdTIXEY52HAzHcfGO3Uu8
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 5

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.spatial.dialect.postgis.PostgisDialect
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
